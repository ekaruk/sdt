# Описание проекта: Stepik-Boomstream Integration Platform

## Общая информация

**Название проекта:** Stepik-Boomstream Integration Platform v2  
**Тип:** Образовательная платформа с интеграцией видеохостинга  
**Технологический стек:**
- Backend: Python 3.x, Flask
- Database: PostgreSQL / SQLite (с поддержкой миграции)
- Телеграм: python-telegram-bot
- API клиенты: Stepik API, Boomstream API
- Deploy: Render.com (Web Service + Cron Jobs)

---

## Назначение системы

Платформа представляет собой комплексное решение для автоматизации доступа к образовательному контенту, которое:

1. **Синхронизирует прогресс обучения** — отслеживает прохождение уроков студентами на платформе Stepik
2. **Управляет доступом к видеоматериалам** — автоматически открывает доступ к защищенным видео на Boomstream при завершении соответствующих уроков
3. **Предоставляет web-интерфейс** — личный кабинет студента с отображением прогресса и списком доступных уроков
4. **Интегрируется с Telegram** — бот для навигации по курсу и запуска веб-приложений
5. **Обеспечивает авторизацию** — множественные способы входа (email/пароль, Telegram, Google OAuth)

---

## Архитектура проекта

### 1. Основные компоненты

#### 1.1 Web Application (Flask)
**Файл:** `web_app.py`

Основное Flask-приложение, предоставляющее:
- Маршруты авторизации и аутентификации
- Личный кабинет пользователя
- Dashboard с прогрессом по курсу
- Admin панель для управления пользователями
- API endpoints для Telegram WebApp

**Основные роуты:**
- `/login` — авторизация пользователя
- `/login/telegram/callback` — callback для Telegram Login Widget
- `/me` или `/dashboard` — личный кабинет
- `/logout` — выход из системы
- `/admin/*` — административная панель

#### 1.2 Telegram Bot
**Файл:** `bots/bot.py`

Telegram-бот, предоставляющий:
- Древовидное меню навигации по курсу
- Прямые ссылки на видеоуроки Boomstream
- Интеграцию с Telegram WebApp
- Персонализированный доступ на основе прогресса пользователя

**Основные функции:**
- `/start` — запуск бота и отображение главного меню
- Callback handlers для навигации по разделам и урокам
- Динамическая генерация URL для Boomstream с персональными паролями
- Интеграция с базой данных для проверки прав доступа

#### 1.3 Sync Service (Синхронизация)
**Файл:** `sync_runner.py`, `app/sync_service.py`

Сервис синхронизации, который:
- Получает список студентов курса через Stepik API
- Проверяет прогресс прохождения уроков
- Автоматически выдает доступ к видео в Boomstream
- Записывает информацию о предоставленных доступах в БД

**Логика работы:**
1. Получение enrollments из Stepik (зачисленные на курс)
2. Для каждого студента проверка прогресса по урокам
3. Если урок завершен (`is_passed = true`) — запись в таблицу `video_grants`
4. Вызов Boomstream API для открытия доступа (текущая версия — заглушка)

---

### 2. Структура базы данных

#### Таблица `users`
Основная таблица пользователей с множественными способами идентификации:

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer PK | Уникальный идентификатор |
| email | String (unique) | Email пользователя из Boomstream |
| first_name | String | Имя |
| last_name | String | Фамилия |
| boom_password | String(32) | 9-значный пароль для доступа к Boomstream |
| stepik_user_id | BigInteger (unique) | ID пользователя в Stepik |
| telegram_id | BigInteger (unique) | Telegram ID |
| google_sub | String | Google OAuth идентификатор |
| video_access | Integer | Уровень доступа к видео (0-100) |
| stepik_lesson_id | Integer | Текущий урок в Stepik |
| stepik_step_id | Integer | Текущий шаг в уроке |
| created_at | DateTime | Дата создания записи |

#### Таблица `video_grants`
Журнал предоставленных доступов к видеоматериалам:

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer PK | Уникальный идентификатор |
| user_id | Integer FK(users.id) | ID пользователя |
| stepik_lesson_id | Integer | ID урока в Stepik |
| boomstream_resource_id | String | Код видео в Boomstream |
| created_at | DateTime | Дата предоставления доступа |

#### Таблица `stepik_modules`
Структура курса — модули (разделы):

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer PK | ID модуля в Stepik |
| title | String | Название раздела |
| position | Integer | Порядковый номер |

#### Таблица `stepik_lessons`
Структура курса — уроки:

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer PK | ID урока в Stepik |
| module_id | Integer FK(stepik_modules.id) | ID родительского модуля |
| title | String | Название урока |
| position | Integer | Порядковый номер в модуле |
| boom_media | String | Код видео в Boomstream (например, "HOP3CJoH") |
| steps_amount | Integer | Количество шагов в уроке |

#### Таблица `telegram_users`
Данные пользователей Telegram (отдельная таблица):

| Поле | Тип | Описание |
|------|-----|----------|
| id | BigInteger PK | Telegram ID |
| first_name | String | Имя в Telegram |
| last_name | String | Фамилия в Telegram |
| username | String | Username в Telegram |
| phone | String | Номер телефона |

---

### 3. Интеграции

#### 3.1 Stepik API Integration
**Файл:** `app/stepik_client.py`

**Методы авторизации:** OAuth2 Client Credentials  
**Основные эндпоинты:**
- `/api/course-enrollments` — список зачисленных на курс
- `/api/users/{user_id}` — информация о пользователе
- `/api/progresses/{progress_id}` — прогресс прохождения шага

**Workflow проверки прогресса:**
1. Получить steps урока
2. Для каждого step получить progress_id
3. Проверить `is_passed` в progresses
4. При полном прохождении — открыть доступ к видео

#### 3.2 Boomstream API Integration
**Файл:** `app/boomstream_client.py`

**Текущий статус:** Заглушка (stub implementation)  
**Планируемый функционал:**
- Предоставление доступа к видеоресурсам по email
- Управление паролями доступа
- Отзыв доступа при необходимости

**Формат видео:**
- Код видео: например `HOP3CJoH`, `TsQAJHvj`
- URL для встраивания: `https://boomstream.com/iframe/{code}`
- URL для просмотра: `https://play.boomstream.com/{code}?id_recovery={password}`

#### 3.3 Telegram Integration
**Способы авторизации:**
1. **Telegram Login Widget** — веб-авторизация с проверкой подписи
2. **Direct Bot Authentication** — авторизация через личные сообщения боту
3. **Telegram WebApp** — встроенные веб-приложения

---

## Основные сценарии использования

### Сценарий 1: Новый студент
1. Студент регистрируется на курсе в Stepik
2. Администратор создает учетную запись в системе (email + boom_password)
3. Студент проходит уроки в Stepik
4. Sync Service автоматически открывает доступ к видео
5. Студент получает доступ через web-интерфейс или Telegram-бота

### Сценарий 2: Просмотр видео через Telegram
1. Студент запускает бота командой `/start`
2. Выбирает раздел курса из меню
3. Выбирает конкретный урок
4. Бот генерирует персональную ссылку с паролем
5. Студент переходит на Boomstream и смотрит видео

### Сценарий 3: Работа через web-интерфейс
1. Студент входит через `/login` (email/пароль или Telegram)
2. Открывает личный кабинет `/dashboard`
3. Видит список доступных уроков и свой прогресс
4. Кликает на урок и смотрит видео во встроенном iframe

---

## Технические особенности

### Безопасность
- **Session Management:** Использование Flask Sessions с `permanent=True`
- **Password Hashing:** Хеширование паролей с использованием werkzeug
- **Telegram Auth:** Проверка подписи данных от Telegram Login Widget
- **OAuth2:** Интеграция с Stepik и Google через OAuth2

### Масштабируемость
- **Database:** Поддержка PostgreSQL для production (Render.com)
- **SQLite:** Для локальной разработки
- **SQLAlchemy ORM:** Абстракция от конкретной СУБД

### Deployment (Render.com)
**Web Service:**
- Build: `pip install -r requirements.txt`
- Start: `gunicorn web_app:app`

**Cron Job (Sync Service):**
- Build: `pip install -r requirements.txt`
- Start: `python sync_runner.py`
- Schedule: настраивается в Render (например, каждые 15 минут)

---

## Структура файлов проекта

```
stepik-boomstream-v2/
├── app/                        # Основное приложение
│   ├── __init__.py            # Фабрика Flask app
│   ├── auth.py                # Логика авторизации
│   ├── boomstream_client.py   # Клиент Boomstream API
│   ├── config.py              # Конфигурация из env
│   ├── db.py                  # Настройка БД и сессий
│   ├── models.py              # SQLAlchemy модели
│   ├── stepik_client.py       # Клиент Stepik API
│   ├── sync_service.py        # Логика синхронизации
│   ├── telegram_auth.py       # Telegram авторизация
│   └── routes/                # Маршруты Flask
│       ├── auth.py            # Роуты авторизации
│       ├── dashboard.py       # Личный кабинет
│       └── webapp_admin.py    # Админ-панель
├── bots/                      # Telegram боты
│   ├── bot.py                 # Production бот
│   ├── bot_dev.py             # Development бот
│   └── menu_tree.py           # Структура меню бота
├── stepik/                    # Утилиты для Stepik
│   ├── stepik_api.py          # Дополнительные API методы
│   ├── sync_stepik_structure.py  # Синхронизация структуры курса
│   ├── fill_stepik_user_ids.py   # Заполнение ID пользователей
│   └── fill_video_access.py      # Заполнение доступа к видео
├── utils/                     # Вспомогательные скрипты
│   ├── create_tables.py       # Создание таблиц БД
│   ├── import_boom_users.py   # Импорт пользователей Boomstream
│   ├── import_telegram_users.py  # Импорт пользователей Telegram
│   └── dump_load.py           # Бэкап и восстановление БД
├── backup/                    # Резервные копии БД
├── templates/                 # HTML шаблоны (если используются)
├── bot.py                     # Точка входа для бота
├── web_app.py                 # Точка входа для веб-приложения
├── sync_runner.py             # Запуск синхронизации
├── requirements.txt           # Python зависимости
└── README.md                  # Документация проекта
```

---

## Переменные окружения

### Обязательные
- `SECRET_KEY` — секретный ключ для Flask сессий
- `DATABASE_URL` — строка подключения к БД (PostgreSQL/SQLite)

### Для Stepik Integration
- `STEPIK_CLIENT_ID` — OAuth2 Client ID
- `STEPIK_CLIENT_SECRET` — OAuth2 Client Secret
- `STEPIK_COURSE_ID` — ID курса в Stepik

### Для Boomstream Integration
- `BOOMSTREAM_API_KEY` — API ключ Boomstream

### Для Telegram Integration
- `TELEGRAM_BOT_TOKEN` — токен основного бота
- `TELEGRAM_BOT_VIDEO_TOKEN` — токен для видео-бота (если отдельный)

### Опциональные
- `GOOGLE_CLIENT_ID` — для Google OAuth (если используется)
- `GOOGLE_CLIENT_SECRET` — для Google OAuth

---

## Локальный запуск

### 1. Установка зависимостей
```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

pip install -r requirements.txt
```

### 2. Настройка переменных окружения
Создать файл `.env` или экспортировать переменные:
```bash
export SECRET_KEY="dev-secret-key"
export DATABASE_URL="sqlite:///local.db"
export STEPIK_CLIENT_ID="your_client_id"
export STEPIK_CLIENT_SECRET="your_client_secret"
export STEPIK_COURSE_ID="123456"
export TELEGRAM_BOT_TOKEN="123456789:AAAA-BBB..."
```

### 3. Создание таблиц БД
```bash
python utils/create_tables.py
```

### 4. Запуск веб-приложения
```bash
python web_app.py
```
Приложение доступно по адресу: http://127.0.0.1:5000

### 5. Запуск синхронизации (опционально)
```bash
python sync_runner.py
```

### 6. Запуск Telegram бота (опционально)
```bash
python bot.py
# или для разработки
python -m bots.bot_dev
```

---

## Текущий статус проекта

### Реализовано ✅
- ✅ База данных с полной структурой таблиц
- ✅ Stepik API интеграция (получение студентов, проверка прогресса)
- ✅ Flask веб-приложение с авторизацией
- ✅ Telegram бот с древовидной навигацией
- ✅ Sync Service для автоматической синхронизации
- ✅ Множественные способы авторизации (email, Telegram, Google)
- ✅ Личный кабинет с отображением прогресса
- ✅ Утилиты для импорта и экспорта данных

### В разработке / Требует доработки 🔄
- 🔄 Boomstream API интеграция (сейчас заглушка)
- 🔄 Админ-панель для управления пользователями
- 🔄 Расширенная аналитика и отчетность
- 🔄 Email уведомления при открытии доступа
- 🔄 Мобильная адаптация веб-интерфейса

### Планируется 📋
- 📋 Интеграция платежной системы
- 📋 Система тестирования и квизов
- 📋 Сертификаты о прохождении курса
- 📋 Gamification элементы (баллы, достижения)

---

## Особенности реализации

### 1. Гибкая авторизация
Система поддерживает несколько способов входа с единой базой пользователей:
- Email + пароль (Boomstream credentials)
- Telegram Login Widget с проверкой подписи
- Google OAuth2 (частично реализовано)

Все способы записывают в сессию:
- `session["user_id"]` — ID пользователя
- `session["auth_method"]` — способ авторизации
- `session.permanent = True` — постоянная сессия

### 2. Персонализированные ссылки на видео
Каждый пользователь получает уникальные ссылки на Boomstream:
```
https://play.boomstream.com/{boom_media}?id_recovery={boom_password}
```
Где:
- `boom_media` — код видео из таблицы `stepik_lessons`
- `boom_password` — персональный 9-значный пароль из таблицы `users`

### 3. Древовидная навигация в боте
Telegram бот использует структуру:
```
root
├── sec_561993 (Раздел 1)
│   ├── lesson_1
│   ├── lesson_2
│   └── ...
├── sec_579296 (Раздел 2)
│   └── ...
└── ...
```

Реализовано через callback_data в InlineKeyboardButton с префиксами:
- `sec_` — раздел курса
- `lesson_` — конкретный урок
- `back_` — возврат назад

---

## Заключение

Проект представляет собой полнофункциональную образовательную платформу, объединяющую:
- 📚 **Stepik** — для структурированного обучения и отслеживания прогресса
- 🎥 **Boomstream** — для защищенной доставки видеоконтента
- 💬 **Telegram** — для удобного доступа к материалам
- 🌐 **Web-интерфейс** — для детального просмотра прогресса

Система автоматизирует процесс предоставления доступа к видеоматериалам на основе прогресса обучения, что значительно упрощает администрирование и улучшает пользовательский опыт.

**Целевая аудитория:** Образовательные организации и авторы курсов, использующие Stepik для структуры курса и желающие интегрировать защищенный видеоконтент с автоматическим управлением доступом.

**Ключевые преимущества:**
- 🤖 Автоматизация управления доступом
- 🔐 Многоуровневая безопасность
- 📱 Мультиплатформенность (Web + Telegram)
- 📊 Отслеживание прогресса в реальном времени
- 🔄 Синхронизация данных между платформами
